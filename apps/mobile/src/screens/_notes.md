Mobile v1 notes

- City search is expected to be backed by `/geo/cities?q=...` (backend proxy to BAN).
- Deep links resolve `https://eatsafe.adabin.fr/r/:publicId` to `/restaurants/public/:publicId`.
- Guest mode hides reviews/photos/opening hours and disables visited/rating/comment actions.
